{
  "name": "fianlterm_report_draft",
  "version": "0.1.0",
  
  // 👈 這一行就是 "homepage"
  "homepage": "https://zxcvbnm010043-bot.github.io/fianlterm_report_draft", 
  
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "recharts": "^2.10.3",
    // ... 其他套件
  },
{
  // ... 其他內容
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",      // 👈 這就是 "build" 腳本的定義
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build"        // 👈 這就是 "deploy" 腳本的定義
  },
  // ... 其他內容
}
    // ...
  },
  // ... 其他內容
}
